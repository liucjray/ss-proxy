http_port ${SQUID_PORT}

# AUTH_CONFIG_PLACEHOLDER
# This section will be replaced by start.sh based on ENABLE_AUTH environment variable

cache deny all
access_log /var/log/squid/access.log squid
pid_filename /var/run/squid.pid